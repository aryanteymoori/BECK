@page
@model ServiceHost.Areas.Administration.Pages.Inventory.CreateInventoryModel
@{
    string message = Model.Message;
}
<div>
    @if (!string.IsNullOrWhiteSpace(message))
    {
        <p class="bg-danger text-dark mx-auto text-center p-4">@message</p>
    }
</div>
<form asp-action="post">
    <div class="card shadow">
        <div class="card-header">
            <h2 class="mb-0">تعریف انبار جدید</h2>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-6">
                    <div class="form-group">
                        <label class="form-label">محصول</label>
                        <select class="form-control select2 w-100" asp-items="@Model.Products" asp-for="CreateInventory.ProductId">
                        </select>
                        <span class="text-danger" asp-validation-for="CreateInventory.ProductId"></span>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        <label class="form-label">قیمت واحد</label>
                        <input type="text" class="form-control is-valid state-valid"
                               asp-for="CreateInventory.UnitPrice">
                        <span class="text-danger" asp-validation-for="CreateInventory.UnitPrice"></span>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <input type="submit" class="btn btn-success" value="ایجاد" />
</form>

